{
  "name": "project-h-backend",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm install"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
